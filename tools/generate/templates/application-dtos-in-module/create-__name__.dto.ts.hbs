import { IsString, IsNotEmpty, IsOptional } from 'class-validator';

export class Create__Name__Dto {
  // POR PADRÃO, NENHUM CAMPO AQUI. O GERADOR OU VOCÊ VAI ADICIONAR CONFORME A ENTIDADE!
  // Isso resolve "Type 'UpdateWorkflowDto' has no properties in common with type 'Partial<WorkflowEntity>'."
  // porque um DTO vazio *é* compatível com Partial<Entity que é apenas BaseEntity>.
}
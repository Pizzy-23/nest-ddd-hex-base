// src/infrastructure/database/typeorm/entities/__name__.schema.ts.hbs
import { Entity } from 'typeorm'; // Importe Entity
import { BaseTypeOrmSchema } from '@infra/database/typeorm/entities/base-typeorm.schema'; // Seu BaseTypeOrmSchema

@Entity('__names__') // Nome da tabela no banco de dados, assumindo plural minúsculo
export class __Name__Schema extends BaseTypeOrmSchema {
  // TODO: Adicione aqui as colunas específicas desta tabela.
  // Ex:
  // @Column({ type: 'varchar', length: 255, unique: true, nullable: false })
  // public name: string;
  //
  // @Column({ type: 'text', nullable: true })
  // public description?: string;
  //
  // TODO: Adicione aqui outras relações com outros schemas.
  // Ex:
  // @ManyToOne(() => UserSchema)
  // @JoinColumn({ name: 'userId' })
  // user?: UserSchema;
}
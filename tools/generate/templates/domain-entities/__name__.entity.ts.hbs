import { BaseEntity as DomainBaseEntity } from '@domain/entities/base-entity'; // Sua BaseEntity de domínio

export class __Name__Entity extends DomainBaseEntity {
  constructor() {
    super(); // Chama o construtor da BaseEntity para inicializar id, createdAt, updatedAt
  }

  // TODO: Adicione aqui as propriedades específicas de domínio desta entidade.
  // Ex:
  // public name: string;
  // public description?: string;
  // public userId?: string; // Para relações (Ex: Se Product tem um owner)

  // TODO: Adicione métodos de negócio (comportamentos da entidade) aqui.
  // Ex:
  // public changeStatus(newStatus: string): void {
  //   this.status = newStatus;
  //   this.updateTimestamps();
  // }
}
import { __Name__Entity } from '@domain/entities/__name__.entity';
import { BaseRepository } from '@domain/repositories/base.repository'; 

// Esta interface provê o contrato para as operações de persistência
// específicas da entidade __Name__. Ela estende o BaseRepository,
// o que significa que já terá os métodos CRUD básicos (save, findById, findAll, delete, edit).
export abstract class __Name__Repository extends BaseRepository<__Name__Entity> {
  // TODO: Adicione aqui métodos de consulta ou modificação específicos
  // que o domínio de __Name__ precisa e que não são cobertos pelo BaseRepository.
}